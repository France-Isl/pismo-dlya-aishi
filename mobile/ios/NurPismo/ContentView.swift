import SwiftUI

struct ContentView: View {
    var body: some View {
        WebViewContainer()
            .background(Color(red: 23 / 255, green: 23 / 255, blue: 34 / 255))
            .ignoresSafeArea()
    }
}

